on join:
	if {joined::%player's uuid%} is not set:
		message "<red>%player%<green> joined for the first time!"
		teleport player to {spawn}
		set {joined::%player's uuid%} to 1
		stop
	message "<green>Welcome %player% to the server!"